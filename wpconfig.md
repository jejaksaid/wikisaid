setting instalasi wp local
use xampp for linux (easiest way)
create database and set database name
in default lampp setting wp-config.php:

yourDatabaseName = database
root             = username
(blank)          = password
localhost        = localhost
