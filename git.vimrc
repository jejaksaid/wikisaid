# initfile
git init
# addingfile 
git add .
#comtting file
git commit -m "commit"
#pushingfile
git push 

#from /opt/lamp give permission first
1. change the ownership of htdocs
sudo chown -R username:username /opt/lampp.../apache2/htdocs
2. change the file persmissions of htdocs folder
sudo chmod -R 775 /opt/lampp.../apache2/htdocs/

