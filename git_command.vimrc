git clone git@github.com:username/reponame.git
git init
git add .
git remote add origin remoteRepoName(URL or ssh)
git commit -m "commit comment"
git push 

working with branch
git checkout -b branchName
git checkout master
git branch
git merge repoName --no-ff
git push

git branch -d repoName
